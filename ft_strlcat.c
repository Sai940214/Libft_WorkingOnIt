#include "libft.h"

size_t ft_strlcat(char * restrict dst, const char * restrict src, size_t dstsize)
{
    size_t  i;

    if (!dst)
    return (0);
    i = 0;
    while (dst[i] != '\0')
    {
        i++;
        
    }

}